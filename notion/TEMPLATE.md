# Notion template

> **Status: coming with v0.1.0.** A public, duplicable Notion page containing all eight databases (with demo rows) will be linked here. Until then, build the databases by hand from [schemas/](schemas/).

Once available:

1. Open the template link and click **Duplicate** (top right) into your own workspace.
2. Share the duplicated page with your integration (Share → Invite → the integration you created in [docs/setup-notion.md](../docs/setup-notion.md)).
3. Copy each database's ID from its URL into `.env.local`.
